<EditForm Model="@dev" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <div class="form-group">
        <label>First Name :</label>
        <div>
            <InputText @bind-Value="@dev.FirstName" />
            <ValidationMessage For="@(() => dev.FirstName)" />
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label>Last Name :</label>
            <div>
                <InputText @bind-Value="@dev.LastName" />
                <ValidationMessage For="@(() => dev.LastName)" />
            </div>
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label>Email :</label>
            <div>
                <InputText @bind-Value="@dev.Email" />
                <ValidationMessage For="@(() => dev.Email)" />
            </div>
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label>Cell Number :</label>
            <div>
                <InputText @bind-Value="@dev.CellNumber" />
                <ValidationMessage For="@(() => dev.CellNumber)" />
            </div>
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label>Region :</label>
            <div>
                <InputText @bind-Value="@dev.Region" />
                <ValidationMessage For="@(() => dev.Region)" />
            </div>
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label>Form Nuber :</label>
            <div>
                <InputText @bind-Value="@dev.FormNum" />
                <ValidationMessage For="@(() => dev.FormNum)" />
            </div>
        </div>
    </div>


    <button type="submit" class="btn btn-success">
        @ButtonText
    </button>

</EditForm>


@code {
    [Parameter] public Developer dev { get; set; }
    [Parameter] public string ButtonText { get; set; } = "Save";
    [Parameter] public EventCallback OnValidSubmit { get; set; }
}